import Quickshell.Hyprland

// Compatibility component for upstream surfaces. The global namespace is
// owned by Cortetsu so legacy callers cannot register caelestia shortcuts.
GlobalShortcut {
    appid: "cortetsu"
}

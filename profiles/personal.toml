schema = 1
name = "personal"
inherits = ["base"]
enabled_tags = ["scenes", "hyprland", "terminal", "user-shell", "toolkit"]
package_groups = ["personal-shell", "desktop-integrations", "optional-tools"]

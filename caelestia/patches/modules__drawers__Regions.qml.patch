--- a/modules/drawers/Regions.qml
+++ b/modules/drawers/Regions.qml
@@ -70,9 +70,15 @@
         height: panel.height + root.borderThickness
     }
 
+    /*
+     * Bottom Hub: utilities is now an independent bottom popover (see
+     * modules/utilities/Wrapper.qml) that slides fully below the screen
+     * edge on close, just like sidebar. Follow its real y (component
+     * default) instead of pinning the input region to the visible bottom
+     * edge, or a hairline strip stays clickable there while closed.
+     */
     R {
         panel: root.panels.utilities
-        y: root.win.height - height
         height: panel.height * (1 - root.panels.utilities.offsetScale) + root.borderThickness
     }
 

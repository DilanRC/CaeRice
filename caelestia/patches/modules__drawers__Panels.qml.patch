--- a/modules/drawers/Panels.qml
+++ b/modules/drawers/Panels.qml
@@ -171,7 +171,6 @@
         id: utilities
 
         screenState: root.screenState
-        sidebar: sidebar
         popouts: popoutsWrapper.content
 
         anchors.bottom: parent.bottom
@@ -181,19 +180,22 @@
     Toasts.Toasts {
         id: toasts
 
-        anchors.bottom: sidebar.visible ? sidebar.top : utilities.top
+        anchors.bottom: utilities.top
         anchors.right: parent.right
         anchors.margins: Tokens.padding.medium
     }
 
+    // Bottom Hub: notifications open as an independent floating popover
+    // centered above the hub, not as a right-docked column. Both anchors.top
+    // (notifications) and anchors.right (old right-dock) are dropped so the
+    // Wrapper's own implicitWidth/implicitHeight and bottomMargin slide
+    // (see modules/sidebar/Wrapper.qml) fully control its box.
     Sidebar.Wrapper {
         id: sidebar
 
         screenState: root.screenState
 
-        anchors.top: notifications.bottom
-        anchors.bottom: utilities.top
-        anchors.right: parent.right
-        anchors.topMargin: -notifications.anchors.topMargin
+        anchors.horizontalCenter: parent.horizontalCenter
+        anchors.bottom: parent.bottom
     }
 }
